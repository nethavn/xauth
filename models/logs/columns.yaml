# ===================================
#  Column Definitions
# ===================================

columns:

    created_at:
        label     : nethavn.xauth::lang.logs.created_at
        searchable: yes

    provider:
        label     : nethavn.xauth::lang.logs.provider
        searchable: yes

    result:
        label     : nethavn.xauth::lang.logs.result
        searchable: yes

    user_id:
        label   : nethavn.xauth::lang.logs.user_id
        relation: user
        select  : login

    email:
        label     : nethavn.xauth::lang.logs.email
        searchable: yes

    ip:
        label     : nethavn.xauth::lang.logs.ip
        searchable: yes